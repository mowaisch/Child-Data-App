import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const CompareScreen = () => {
    return (
        <View style={styles.topContainerStyles}>
            <Text>ComapreScreen</Text>
        </View>
    );
}

export default CompareScreen;

const styles = StyleSheet.create({
    topContainerStyles: {
        flex: 1
    }
});